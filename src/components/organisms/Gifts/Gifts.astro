---
import Copy from "@components/molecules/Copy/Copy";
import CopyIcon from "@public/icons/copy.svg";
import MoneyIcon from "@public/icons/money.svg";
import BankIcon from "@public/icons/bank.svg";
import flower1Image from "@public/images/flower--1.png";
import { Image } from "astro:assets";

const pagoMovilItems = [
  {
    key: "Banco",
    value: "0105",
    label: "Banco Mercantil (0105)",
  },
  {
    key: "Teléfono",
    value: "04142156802",
    label: "0414-2156802",
  },
  {
    key: "Cédula",
    value: "27794231",
    label: "27.794.231",
  },
];
---

<section class="host">
  <div class="column">
    <h2 class="title font--heading">
      <span>Regalos</span>
    </h2>

    <div class="intro">
      <p class="description">
        <span class="description__block">
          Tu presencia es el mejor regalo de todos. Si deseas impulsar nuestros
          primeros pasos como casados puedes regalarnos algo especial, lo
          recibiremos con mucho amor y gratitud.
        </span>
        <span class="description__block"
          >¡Gracias por tu cariño y apoyo incondicional!</span
        >
      </p>
    </div>
  </div>

  <div class="column">
    <div class="cards">
      <div class="card">
        <Image class={"card__flower"} src={flower1Image} alt={"Flower"} />
        <ul class="list">
          <li class="item">
            <div class="item__header">
              <h4 class="item__title">Dinero en Efectivo</h4>
            </div>
            <p class="item__description">
              Recuerda colocarlo en un sobre, no desperdicies la oportinidad de
              incluir un mensaje personalizado.
            </p>
          </li>
        </ul>
        <MoneyIcon class="icon--card icon--money" width={48} height={48} />
      </div>

      <div class="card">
        <Image class={"card__flower"} src={flower1Image} alt={"Flower"} />

        <ul class="list">
          <li class="item">
            <div class="item__header">
              <h4 class="item__title">Transferencia Bancaria</h4>
              <p class="item__description">
                Realiza tu aporte el día del evento.
              </p>
            </div>
            <ul class="sublist sublist--copy">
              {
                pagoMovilItems.map((item) => (
                  <li class="subitem subitem--copy">
                    <Copy client:visible value={item.value}>
                      <p class="copy">{`${item.key}: ${item.label}`}</p>
                      <CopyIcon class="icon--copy" width={16} height={16} />
                    </Copy>
                  </li>
                ))
              }
            </ul>
          </li>
        </ul>
        <BankIcon class="icon--card icon--bank" width={48} height={48} />
      </div>
    </div>
  </div>
</section>

<style>
  .host {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 3rem 1.5rem;

    @media (min-width: 1024px) {
      max-width: 80rem;
      margin: 0 auto;
      padding: 6rem 2rem 8rem;
      gap: 4rem;
    }

    @media (min-width: 85.375rem) {
      padding: 6rem 2rem 10rem;
      gap: 4.5rem;
    }
  }

  .column {
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 1.6rem;

    @media (min-width: 1024px) {
      justify-content: center;
      gap: 2rem;
    }
  }

  .cards {
    width: min(768px, 85%);
    margin: 0 auto;
    display: grid;
    place-items: center;
    gap: 1.5rem;

    @media (min-width: 1024px) {
      grid-template-columns: repeat(2, 1fr);
      gap: 3rem;
    }
  }

  .card {
    position: relative;
    width: 100%;
    display: flex;
    flex-direction: column;
    padding: 1.5rem;
    background-color: var(--primary);
    color: var(--white);
    border-radius: 0.3rem;
    gap: 2rem;
    align-self: center;

    @media (min-width: 1024px) {
      height: 100%;
      padding: 2rem;
    }
  }

  .card__flower {
    position: absolute;
    bottom: calc(100% - 1.2rem);
    right: calc(100% - 1.2rem);
    width: 2.25rem;
    height: auto;
  }

  .title {
    position: relative;
    display: flex;
    font-size: 2.4rem;
    justify-content: center;

    @media (min-width: 768px) {
      font-size: 2.8rem;
    }

    @media (min-width: 1024px) {
      font-size: 3.8rem;
    }
  }

  .intro {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    text-align: center;
  }

  .subtitle {
    font-size: 1.6rem;
    text-align: center;

    @media (min-width: 768px) {
      font-size: 2rem;
    }

    @media (min-width: 1024px) {
      font-size: 2.8rem;
    }
  }

  .subtitle__block {
    display: block;
  }

  .list {
    display: flex;
    flex-direction: column;
    gap: 3rem;
    list-style: none;

    @media (min-width: 1024px) {
      gap: 3.5rem;
    }
  }

  .item {
    font-size: 1.4rem;

    @media (min-width: 768px) {
      font-size: 1.8rem;
    }

    @media (min-width: 1024px) {
      font-size: 2.2rem;
    }
  }

  .description,
  .subitem__description,
  .copy {
    font-size: 1.2rem;
    font-weight: normal;

    @media (min-width: 768px) {
      font-size: 1.6rem;
    }
  }

  .item__title {
    text-align: center;
  }

  .item__description {
    text-align: center;
    font-size: 1rem;

    @media (min-width: 768px) {
      font-size: 1.4rem;
    }
  }
  .description__block {
    display: block;
  }

  .item__header {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 1.2rem;

    @media (min-width: 1024px) {
      margin-bottom: 1.6rem;
      gap: 1rem;
    }
  }

  .sublist {
    gap: 1.6rem;
    list-style-type: disc;
  }

  .sublist--copy {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.6rem;
    list-style: none;
    padding-left: 0;
  }

  .subitem,
  .copy {
    font-size: 1rem;

    @media (min-width: 768px) {
      font-size: 1.4rem;
    }
  }

  .icon--copy {
    color: var(--secondary);

    @media (min-width: 1024px) {
      width: 1.375rem;
      height: 1.375rem;
    }
  }

  .subitem--copy:focus .icon--copy,
  .subitem--copy:hover .icon--copy {
    color: var(--white);
  }

  .icon--card {
    position: absolute;
    top: calc(100% - 2rem);
    left: calc(100% - 2rem);
  }

  .icon--bank {
    background-color: var(--white);
    border-radius: 50%;
  }
</style>
